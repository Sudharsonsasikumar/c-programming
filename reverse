#include<stdio.h>
 int main(){
   int n,rem,rev=0;
   printf("enter the element:");
   scanf("%d",&n);
   while(n!=0)
   {
       rem=n%10;
       rev=rev*10+rem;
       n/=10;
   }
   printf("\n%d",rev );
     return 0;
 }
======================================
output:
enter the element:345

543

=== Code Execution Successful ===
